title Weather Data — Open-Meteo Integration
 
participant Frontend
participant FastAPI
participant Open-Meteo API
participant SQLite
 
Frontend->FastAPI: GET /weather_address\n?address=M5V2T6&days_ahead=7
 
note over FastAPI: Geocode runs first\n(see Diagram 2)
 
FastAPI->Open-Meteo API: GET /v1/forecast\n?latitude={lat}&longitude={lon}\n&hourly=temperature_2m,\nrelativehumidity_2m,\nshortwave_radiation,\nprecipitation,snowfall,\nwindspeed_10m\n&timezone=America/Toronto
 
note over Open-Meteo API: Free — no API key required\nResponses cached 1 hr locally\nby requests-cache
 
Open-Meteo API-->FastAPI: Raw NWP JSON\n(168 hourly rows for 7 days)
 
FastAPI->FastAPI: Build pandas DataFrame\nConvert UTC → America/Toronto\nFilter today → today + 7 days\nSerialise rows to JSON dicts
 
FastAPI->SQLite: UPDATE users SET\nuser_weather = JSON(snapshot),\nweather_date = today\nWHERE username = ?
 
SQLite-->FastAPI: OK
 
FastAPI-->Frontend: 200 {rows:[{date,\ntemperature_2m, humidity,\nsolar_radiation, windspeed,\nprecipitation, snowfall}...],\nlat, lon, start_date}