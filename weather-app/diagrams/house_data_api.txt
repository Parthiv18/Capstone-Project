title House Variables — Setup & Storage
 
participant Frontend
participant FastAPI
participant SQLite
 
Frontend->FastAPI: POST /house_variables\n{username, home_size,\ninsulation_quality, hvac_type,\npersonal_comfort, occupancy,\nappliances:[...]}
 
FastAPI->SQLite: UPDATE users\nSET user_house = JSON({\n  data: {home_size, insulation,\n         hvac_type, comfort},\n  appliances: [...]\n})\nWHERE username = ?
 
SQLite-->FastAPI: OK
 
FastAPI->SQLite: SELECT sim_inside_temp\nFROM user_thermostat\nWHERE user_id = ?
 
SQLite-->FastAPI: null (first time)
 
FastAPI->SQLite: SELECT temperature_2m\nFROM first weather row
 
SQLite-->FastAPI: e.g. −5.2°C
 
FastAPI->SQLite: INSERT INTO user_thermostat\n(user_id, sim_inside_temp)\nON CONFLICT DO UPDATE
 
SQLite-->FastAPI: OK
 
FastAPI-->Frontend: 200 {status:"ok"}
 
note over FastAPI,SQLite: sim_inside_temp seeds the RC model\nwith real outdoor temp at signup.\nAll future steps UPDATE this row.